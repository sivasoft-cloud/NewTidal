{
  "build": {
    "env": {
      "API_BASE": "https://tidal.401658.xyz"
    },
    "start": "flask run --host 0.0.0.0 --port $PORT"
  },
  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}