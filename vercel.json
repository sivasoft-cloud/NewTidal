{
  "build": {
    "command": "pip install -r requirements.txt && flask run",
    "env": {
      "API_BASE": "https://tidal.401658.xyz"
    }
  },
  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}